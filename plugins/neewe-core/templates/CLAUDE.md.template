# CLAUDE.md — Project Doctrine (NEEWE-managed)

> Auto-generated by `neewe l4-init`. Edit freely; this is YOUR project doctrine layered on top of NEEWE-Core.

---

## Project Identity

- **Name:** <PROJECT_NAME>
- **Stack:** <auto-detected or hand-filled>
- **Phase:** see `.neewe/state.json#phase`
- **Active goal:** see `.neewe/state.json#active_goal`

(Run `neewe show` to inspect runtime state.)

---

## NEEWE Doctrine (active in this project)

This project uses the **NEEWE Framework** (NeeweTec/neewe-ai-marketplace). All NEEWE doctrine applies:

- **Karpathy 4 principles** — Think Before Coding / Simplicity First / Surgical Changes / Goal-Driven Execution
- **Cardinal Rules** — see the `using-neewe` skill (loaded by SessionStart hook on every session)
- **Tone Spec** — NEEWE Startup output style (active by default; see `output-styles/neewe-startup.md`)
- **Triple-gate governance** — QA + Tech Lead + PO at every stage transition
- **Cost-capped Goal Mode** — `cost-cap-guard` Stop hook enforces `cost_budget.cap_usd`

---

## L4 Memory Consultation Rule (MNEME)

**Before grepping code or answering "why" questions, consult MNEME in this order:**

1. **T1 Hot (CLAUDE.md)** — this file + nested per-directory CLAUDE.md files
2. **T2 Vault (`.neewe/vault/`)** — concept pages, entity catalog, ADRs, source summaries
3. **T3 Graph (`.neewe/graph/`)** — structural queries (`who calls X?`, `what depends on Y?`) via Graphify MCP (when installed)
4. **Raw code** — only as last resort

After answering: **fold the answer back into the vault** at `.neewe/vault/<appropriate-folder>/<topic>.md`. Future sessions don't re-derive what's been answered.

---

## Project-Specific Conventions

(Add your project's specific rules here. Examples:)

- **Test command:** `npm test` (CI runs on `npm run ci`)
- **Lint command:** `npm run lint`
- **Build command:** `npm run build`
- **Deployment:** auto on tag push to `main`
- **Database migrations:** never auto-apply in `03-execute` phase; always require user approval

---

## Path-Scoped Rules

Rules with `paths:` frontmatter live in `.claude/rules/`. Loaded only when Claude touches a matching file:

- `.claude/rules/api-style.md` — paths: `src/api/**/*.ts`
- `.claude/rules/testing.md` — paths: `**/*.test.ts`
- (add more as needed)

---

## Project Decisions (top 3 — full list in vault)

(Latest 3 architectural decisions for hot context; full list at `.neewe/vault/decisions/`.)

1. _(decision)_ — _(rationale + date + decided_by)_
2. ...
3. ...

---

## Active Squad

Run `neewe-squad-composer` to assemble or reassess the squad. Current squad declared at `.neewe/state.json#active_squad`.

---

## Cost Discipline

- Active cap: see `.neewe/state.json#cost_budget.cap_usd`
- Current spend: `neewe cost-report`
- Mode: see `.neewe/state.json#mode` (thrift / balanced / quality)

When in doubt, default to `balanced` mode. Switch to `quality` only for security-sensitive work or deep architecture decisions.

---

## Imports (loaded into context with this file)

Recommended imports for NEEWE projects:

```
@.claude/rules/neewe-core.md
@.claude/rules/<project-specific>.md
```

---

*This file is the T1 hot tier of MNEME. Keep it small (target <4K tokens). Move heavy content to `.neewe/vault/` (T2) and reference by `[[wikilinks]]`.*
