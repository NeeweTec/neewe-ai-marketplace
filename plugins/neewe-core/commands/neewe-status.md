---
description: Show the current NEEWE stage, cost, gates, and recent events as a rich markdown view.
allowed-tools: Bash(${CLAUDE_PLUGIN_ROOT}/bin/neewe inline)
---

Run the NEEWE inline status view and present it directly to the user (no commentary).

!`bash ${CLAUDE_PLUGIN_ROOT}/bin/neewe inline`
